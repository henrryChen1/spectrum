server.port: 8001
mybatis.type-aliases-package: com.plkj.spectrum.bean
mybatis.mapper-locations: classpath:mapper/*.xml
# hive 数据源自定义配置
spring:
  datasource:
    url: jdbc:hive2://10.10.10.104:10000/cw
    username: cw
    password: cw
    driver-class-name: org.apache.hive.jdbc.HiveDriver

logging:
  level:
    com.plkj.spectrum.dao: debug